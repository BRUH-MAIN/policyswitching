# 2026-09-12 — cluster

Session host: `asaicomputemaster`. It is the login node *and* the RTX 6000 Ada compute node; it's currently drained like the others.

## Setup
- Merged `ffe0891` (coordination kit) into local branch `review-2026-09-12` rather than checking out `main`. That branch holds `11c4c4d` (the design-review changes: pinned eval, eval matrix, Generalist/GapsWarm tasks), which queued jobs 11918/11919 need on disk. **Not on `origin/main` yet** — pending a decision on pushing it.
- `.claude/settings.local.json` installed from `coordination/settings-examples/settings.local.cluster.json`.
- `coordination/inbox/to-cluster.md`: empty.

## sbatch submissions (made before the kit landed; logged here per docs/CLAUDE.cluster.md)

| job | command | what it tests | config | walltime |
|---|---|---|---|---|
| 11918 | `SPEC=Generalist sbatch a100/train_specialist_slurm.sh` | Sensing-matched generalist (arm 1 in revised `objective.md`) | `Unitree-Go2-Generalist`: stock PPO over all four terrain classes at equal class weight; identical obs/rewards/runner to specialists; 10k-iteration budget, seed 42, 8192 envs; no `HF_TOKEN` → local disk only | 1 day, `gpu:a100:1` |
| 11919 | `sbatch a100/eval_matrix_slurm.sh` | Premise gates: do specialists degrade off-terrain; do policies use the height scan | `a100/eval_matrix.py`, 87 cells: flat/rough/stairs specialists + PAS stage2 (oracle, estimator-only) × {flat, rough, stairs, gaps, mixed} × difficulty {0.25, 0.5, 0.75}, + height-scan ablation at 0.5; 1024 envs × 1200 steps; terrain curriculum off. Resumable | 12 h, `gpu:1` (any type) |

## Job state
- **11914** (`go2_spec_gaps`, attempt 3, blended terrain): PENDING, never started, no log or checkpoint. `coordination/status/cluster.json` entry left as-is (`in_progress`, iteration null).
- 11918, 11919: PENDING.
- Cause for all three: every node `drained` since 2026-09-10 (`sinfo -R`: "Kill task failed"). They start when an admin resumes the nodes.
- 11919's eval subprocesses import `eval_checkpoint.py` from disk per cell, so the branch must stay checked out until that job *finishes*, not just starts.
